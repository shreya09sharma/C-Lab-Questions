#include <stdio.h>

int main() {
    // Write C code here 
int num;
int sum=0;
    printf("enter a number\n");
    scanf("%d",&num);
    while(num>0)
    {
        int d=num%10;
        sum=sum+d;
        num=num/10;
    }
    printf("The sum of digits %d \n", sum);
    
    return 0;
}
