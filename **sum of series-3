#include <stdio.h>

int main() {
    int n;
    double fact = 1, sum = 0;

    printf("Enter the value of n: ");
    scanf("%d", &n);

    for (int i = 1; i <= n; i++) {
        fact = fact * i;   
        sum = sum + fact;
    }

    printf("Sum of the series = %d\n", sum);

    return 0;
}
